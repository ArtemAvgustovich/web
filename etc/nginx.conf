user http;
worker_processes 1;

http {
	include mime.types;
	server {

		listen       80;
	
		location ^~ /uploads/ {
			root /home/box/web;
		}
		
		location ~* \.(.*)$ {
		root /home/box/web/public;
		}
	}
}
